# 互動拍照活動整合紀錄

更新日期：2026-08-18（Asia/Taipei）

## 專案基準

- 正式設定來源：Supabase `douhmxipedgpfbvfynbq` → `tenants.config_json.photoActivity`。
- 正式設定尚未存在時，只顯示已確認的英式花園安全預設，不會因讀取頁面而寫入或改動 `tuibile` 的任何原有設定；只有總管理者明確按下儲存時，才會新增或更新獨立的 `photoActivity` 區塊。
- 存取路徑：2BL 後台 → `2bl-v7` → Supabase；活動網站只讀取公開展示設定。
- 現有活動網站、拍照、相簿、核銷與管理功能維持不變。
- 相片原檔與活動執行紀錄仍由既有活動網站的受管儲存空間保存，不複製進 2BL。

## 已確認

- 2BL「系統設定」新增「互動拍照活動」。
- 原有 11 個設定固定保留原名稱與原順序：信件模板、付款資訊、合約管理、權限管理、公司資料、首頁顯示、主題活動、常用場地圖、活動拍照框、現場通行碼、系統異常。
- 「互動拍照活動」只能作為第 12 個獨立入口放在原有設定之後，不得取代、改名、移位或隱藏原設定。
- 可修改活動名稱、主題、主辦顯示、首頁文字、底圖、顏色、三款相框、同意文字與任務題庫。
- 公開活動網址、公開相簿網址及活動管理入口分開呈現。
- 公開相簿可分享；活動管理台可在電腦下載整本相簿。
- 總管理者可建立業主專用相簿及一鍵下載網址；網址最長開放 30 天，不包含刪照、核銷或設定權限。

## 權限矩陣

| 功能 | 一般參加者 | 業主連結持有人 | 工作人員 | 總管理者 |
|---|---:|---:|---:|---:|
| 拍照／上傳 | 是 | 否 | 否 | 是 |
| 查看及分享公開相簿 | 是 | 是 | 是 | 是 |
| 一鍵下載整本相簿 | 否 | 是（限時網址） | 否 | 是 |
| 核銷花園派對券 | 否 | 否 | 是 | 是 |
| 隱藏／刪除照片 | 否 | 否 | 否 | 是 |
| 修改活動主題與題庫 | 否 | 否 | 否 | 2BL 總管理者 |

## 安全與失敗防護

- 公開設定只回傳活動顯示、任務與相框資料，不回傳活動管理網址、管理密碼、工作人員密碼或任何下載權杖；完整管理入口只能由總管理者登入後讀取。
- 業主下載網址採隨機權杖並設期限；重新建立後舊網址失效。
- 隱藏照片不會被包進整本下載檔。
- 2BL 設定服務暫時無法連線時，活動網站使用目前已確認的英式花園設定，不中斷拍照流程。
- 只有 2BL `superadmin` 能寫入 `photoActivity`；公開端只能讀取已清理的展示欄位。
- 讀取活動設定不得執行任何新增、更新或刪除；儲存時必須先讀回完整原設定，只更新 `photoActivity`，再完整保存，禁止用新活動資料覆蓋整份主辦設定。

## 2026-08-18 原設定復原防護（Verified）

- 工作分支：`agent/restore-settings-photo-activity-20260818`。
- 本次未寫入 Supabase；程式已透過 PR #52 合併 `main`，並依使用者「確認部署」正式發布至 `2bl-v7`。
- 唯讀核對確認 `g.2b-love.com` 仍為公開且有效的正式活動網址，自訂網域與憑證狀態正常，目前活動網站為第 15 版；本次未建立或發布新的活動網站版本。
- 唯讀核對確認 `tuibile` 的信件模板、付款資料、合約、場地圖、活動拍照框、現場通行碼、首頁、銀行、LINE 與公司資料仍存在。
- 新增回歸防護：原有 11 個設定少一個、改名、換順序，或新活動入口沒有放在最後，測試立即失敗。
- 新增資料防護：開啟活動設定不得寫入資料庫；儲存時只能更新獨立活動區塊，不能覆蓋其他原設定。
- 新增管理入口隔離：公開活動端與後台管理端分開讀取，民眾不會取得相簿管理與整本下載入口。
- 正式網站已套用本次復原；Cloudflare、Supabase 唯讀連線、資料契約、角色模擬、Wrangler 預演、部署後來源比對與 GitHub Pages 發布皆通過。

## 驗證紀錄

- 活動網站正式建置與產物驗證通過。
- 2BL Worker JavaScript 語法檢查通過。
- 2BL 後台內嵌程式語法檢查通過。
- 2BL 資料契約檢查通過：沒有新增未知資料表、未知 RPC 或前端直連 Supabase。
- 平台、系列、場次、財務、現場與公開端權限回歸測試通過。
- 前次 `2bl-v7` 部署成功：版本 `aa21a40f-c8fc-4add-a1d6-cf668e87cc18`，部署來源與預期來源 SHA-256 完全一致。
- 本次原設定復原與活動頁隔離已正式部署：版本 `33cbd217-4126-4120-8049-7454016ca306`，GitHub 核准版本 `b811b064318a6e3c3fa283d51557e947687bfc31`，部署後來源比對一致；最新已驗證基準保存於 `1d88ac09a84251ba80b88f898f0b3010044617e7`。
- 正式公開設定 API 已連續讀取兩次，均回傳 `tuibile` 的 3 款相框與 6 組任務，且不包含敏感欄位；第一次讀取已確保正式 Supabase 設定存在。
- 活動網站新版部署成功：`https://garden-photo-challenge.cats59430.chatgpt.site`，自訂網域維持 `https://g.2b-love.com`。
- `tobeloved-api` 未觸碰；`2bl-v7` 路由維持 `2b-love.com/s/*`。

## 正式狀態

- GitHub：`ndiangrace-create/2bL` → `main`。
- 2BL Worker：`2bl-v7`，正式版本已上線。
- 活動網站：新版已發布，公開活動、相簿及管理入口維持原網址。
- 尚待使用者以正式 Google 管理者帳號完成一次「改字 → 儲存 → 活動頁刷新」的人工作業驗收；程式、權限、資料寫入路徑與公開讀取已通過自動測試及正式端讀取驗證。
